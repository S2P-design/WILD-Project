
<div class="hero">
  <img src="/basecamp/brand/logos/wp-monogram-orange.png" alt="Wild Project" class="hero-logo" />
  <h1>
    <span class="hero-word-wild">WILD</span> <span class="hero-word-project">PROJECT</span>
  </h1>
  <div class="hero-profile">
    <img src="/brand-assets/rene-wild.jpg" alt="René Wild" />
  </div>
  <div class="hero-divider" aria-hidden="true"><span></span></div>
</div>

<style>
  .hero {
    display: flex; flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    padding: 22vh 3rem 40px;
    min-height: 62vh;
    position: relative;
    text-align: center;
    background: #F5F5F3;
    z-index: 1;
  }
  .hero-logo {
    width: 3cm;
    height: auto;
    margin-bottom: 20px;
    opacity: 0;
    transform: translateY(-48px);
    animation: heroFromTop 0.8s cubic-bezier(.5,.1,.2,1) 0.2s forwards;
  }
  .hero-profile {
    width: 96px;
    height: 96px;
    border-radius: 50%;
    overflow: hidden;
    margin-top: 36px;
    border: 2px solid var(--orange);
    background: #F5F5F3;
    opacity: 0;
    transform: translateY(20px);
    animation: heroFromBottom 0.9s cubic-bezier(.5,.1,.2,1) 1.4s forwards;
    cursor: pointer;
    transition: box-shadow 0.55s cubic-bezier(.2,.7,.2,1);
    box-shadow:
      0 4px 18px rgba(0,0,0,0.08),
      0 0 0 5px rgba(252,80,0,0.20),
      0 0 0 12px rgba(252,80,0,0.10),
      0 0 0 21px rgba(252,80,0,0.04);
  }
  .hero-profile img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 18%;
    transform: scale(1.05);
    transform-origin: center 20%;
    filter: contrast(1.06) brightness(1.04);
    transition: transform 0.55s cubic-bezier(.2,.7,.2,1), filter 0.45s ease;
    display: block;
  }
  .hero-profile:hover {
    box-shadow:
      0 8px 28px rgba(252,80,0,0.18),
      0 0 0 5px rgba(252,80,0,0.32),
      0 0 0 14px rgba(252,80,0,0.18),
      0 0 0 26px rgba(252,80,0,0.08);
  }
  .hero-profile:hover img {
    transform: scale(1.30);
    filter: contrast(1.1) brightness(1.06);
  }
  @keyframes heroFromBottom {
    to { opacity: 1; transform: translateY(0); }
  }

  /* ── Hero-Divider: orange Linie weiß↔schwarz, animiert ── */
  .hero-divider {
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 0;
    height: 1.5px;
    background: linear-gradient(90deg,
      transparent 0%,
      rgba(252,80,0,0.55) 25%,
      rgba(252,80,0,0.7) 50%,
      rgba(252,80,0,0.55) 75%,
      transparent 100%);
    overflow: hidden;
    pointer-events: none;
    opacity: 0;
    animation: heroLineExpand 1.4s cubic-bezier(.5,.1,.2,1) 1.9s forwards;
  }
  .hero-divider span {
    position: absolute;
    top: -1px;
    left: -20%;
    width: 22%;
    height: 4px;
    background: linear-gradient(90deg,
      transparent 0%,
      rgba(255,170,100,0.95) 50%,
      transparent 100%);
    filter: blur(1.5px);
    animation: heroLineSweep 6.5s ease-in-out 3.4s infinite;
  }
  @keyframes heroLineExpand {
    0%   { width: 0;   opacity: 0; }
    100% { width: 72%; opacity: 1; }
  }
  @keyframes heroLineSweep {
    0%   { left: -22%; opacity: 0; }
    8%   { opacity: 1; }
    92%  { opacity: 1; }
    100% { left: 100%; opacity: 0; }
  }
  @media (max-width: 700px) {
    @keyframes heroLineExpand {
      0%   { width: 0;   opacity: 0; }
      100% { width: 86%; opacity: 1; }
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .hero-divider {
      animation: none;
      width: 72%;
      opacity: 1;
    }
    .hero-divider span { animation: none; opacity: 0; }
  }
  .hero h1 {
    font-size: clamp(2.5rem, 7vw, 5.5rem);
    font-weight: 700;
    letter-spacing: -2px;
    line-height: 1.0;
    color: #1a1a1a;
    margin: 0;
    overflow: hidden;
  }
  .hero h1 .hero-word-wild,
  .hero h1 .hero-word-project {
    display: inline-block;
    opacity: 0;
  }
  .hero h1 .hero-word-wild {
    color: var(--orange);
    transform: translateX(-80px);
    animation: heroFromLeft 0.9s cubic-bezier(.5,.1,.2,1) 0.65s forwards;
  }
  .hero h1 .hero-word-project {
    transform: translateX(80px);
    animation: heroFromRight 0.9s cubic-bezier(.5,.1,.2,1) 0.65s forwards;
  }

  @keyframes heroFromTop {
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes heroFromLeft {
    to { opacity: 1; transform: translateX(0); }
  }
  @keyframes heroFromRight {
    to { opacity: 1; transform: translateX(0); }
  }

  @media (max-width: 700px) {
    .hero {
      padding: 28vh 1.25rem 32px;
      min-height: 56vh;
    }
    .hero h1 {
      letter-spacing: -1.5px;
      font-size: clamp(2.4rem, 11vw, 3.6rem);
      line-height: 0.95;
    }
    .hero-word-wild { transform: translateX(-50px); }
    .hero-word-project { transform: translateX(50px); }
    .hero-logo { transform: translateY(-32px); }
  }
  @media (max-width: 400px) {
    .hero { padding-top: 26vh; }
    .hero h1 { letter-spacing: -1px; font-size: 2.2rem; }
  }
  @media (prefers-reduced-motion: reduce) {
    .hero-logo, .hero-word-wild, .hero-word-project {
      animation: none !important;
      opacity: 1 !important;
      transform: none !important;
    }
  }
</style>
