
<div class="hero">
  <h1>WILD <span>Project</span></h1>
  <p class="hero-aka">aka René <strong>WILD</strong></p>
  <p class="hero-role">Coach · Creator · Podcaster · Ästhetik Junkie</p>
  <p class="hero-sub">
    <span class="word w1">Design</span><span class="dot d1">.</span>
    <span class="word w2">Perform</span><span class="dot d2">.</span>
    <span class="word w3">Succeed</span>
  </p>
  <div class="scroll-hint"><span>Scroll</span></div>
</div>

<style>
  .hero {
    display: flex; flex-direction: column;
    justify-content: flex-start;
    padding: 0 3rem 24px;
    padding-top: 45vh;
    min-height: 100vh;
    position: relative;
  }
  .hero h1 {
    font-size: clamp(2.5rem, 7vw, 5.5rem);
    font-weight: 700;
    letter-spacing: -2px;
    line-height: 1.0;
    color: var(--white);
    margin-bottom: 12px;
    opacity: 0;
    transform: translateY(20px);
    animation: heroFadeUp 0.8s ease 0.3s forwards;
  }
  .hero h1 span { color: var(--orange); }
  .hero-aka {
    font-size: clamp(1rem, 2.5vw, 1.4rem);
    font-weight: 300;
    color: rgba(255,255,255,0.4);
    letter-spacing: 1px;
    margin-bottom: 6px;
    opacity: 0;
    transform: translateY(15px);
    animation: heroFadeUp 0.7s ease 0.6s forwards;
  }
  .hero-aka strong {
    color: var(--white);
    font-weight: 600;
  }
  .hero-role {
    font-size: clamp(0.65rem, 1.5vw, 0.85rem);
    font-weight: 300;
    color: rgba(255,255,255,0.25);
    letter-spacing: 0.5px;
    margin-bottom: 16px;
    opacity: 0;
    transform: translateY(12px);
    animation: heroFadeUp 0.7s ease 0.8s forwards;
  }
  .hero-sub {
    display: flex; align-items: center; gap: 10px;
    font-size: 13px;
    color: rgba(255,255,255,0.4);
    font-weight: 300;
    letter-spacing: 3px;
    text-transform: uppercase;
  }
  .hero-sub::before {
    content: '';
    display: inline-block;
    width: 0; height: 1.5px;
    background: var(--orange);
    animation: dashGrow 1.2s ease 0.6s forwards;
  }
  .hero-sub .word {
    opacity: 0;
    animation: wordFade 0.6s ease forwards;
  }
  .hero-sub .w1 { animation-delay: 1.2s; }
  .hero-sub .w2 { animation-delay: 1.6s; }
  .hero-sub .w3 { animation-delay: 2.0s; }
  .hero-sub .dot {
    color: var(--orange);
    opacity: 0;
    animation: wordFade 0.3s ease forwards;
  }
  .hero-sub .d1 { animation-delay: 1.5s; }
  .hero-sub .d2 { animation-delay: 1.9s; }

  /* ── SCROLL INDICATOR ──────────────── */
  .scroll-hint {
    margin-top: 24px;
    display: flex; flex-direction: column; align-items: center; gap: 6px;
    opacity: 0;
    animation: hintFadeIn 0.6s ease 2.8s forwards;
  }
  .scroll-hint span {
    font-size: 8px; letter-spacing: 3px; text-transform: uppercase;
    color: rgba(255,255,255,0.2);
    font-weight: 400;
  }
  .scroll-hint::after {
    content: '';
    width: 1px; height: 24px;
    background: linear-gradient(to bottom, var(--orange), transparent);
    animation: scrollPulse 2s ease-in-out 3s infinite;
  }

  @keyframes heroFadeUp { to { opacity: 1; transform: translateY(0); } }
  @keyframes dashGrow { to { width: 24px; } }
  @keyframes wordFade { to { opacity: 1; } }
  @keyframes hintFadeIn { to { opacity: 1; } }
  @keyframes scrollPulse {
    0%, 100% { opacity: 0.3; transform: scaleY(1); }
    50% { opacity: 0.8; transform: scaleY(1.3); }
  }

  @media (max-width: 700px) {
    .hero { padding: 0 1.5rem 0; min-height: 100svh; justify-content: flex-start; padding-top: 52vh; }
    .scroll-hint { margin-top: 20px; opacity: 1; animation: none; }
    .scroll-hint::after { animation: scrollPulse 2s ease-in-out infinite; }
    .hero h1 { letter-spacing: -2px; font-size: clamp(2rem, 10vw, 3.5rem); margin-bottom: 6px; }
    .hero-aka { margin-bottom: 4px; font-size: 0.9rem; }
    .hero-role { font-size: 0.6rem; margin-bottom: 10px; }
    .hero-sub { font-size: 10px; letter-spacing: 2px; gap: 6px; }
  }
  @media (max-width: 400px) {
    .hero h1 { letter-spacing: -1px; }
  }
</style>
