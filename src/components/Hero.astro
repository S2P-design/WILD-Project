
<div class="hero">
  <img src="/basecamp/brand/logos/wp-monogram-orange.png" alt="Wild Project" class="hero-logo" />
  <h1>
    <span class="hero-name-wild">WILD</span> PROJECT
  </h1>
  <div class="scroll-hint"><span>Scroll</span></div>
</div>

<style>
  .hero {
    display: flex; flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    padding: 0 3rem 40px;
    padding-top: 22vh;
    min-height: 62vh;
    position: relative;
    text-align: center;
    background: #F5F5F3;
    z-index: 1;
  }
  .hero-logo {
    width: 3cm;
    height: auto;
    margin-bottom: 20px;
    opacity: 0;
    transform: translateY(16px);
    animation: heroFadeUp 0.7s ease 0.2s forwards;
  }
  .hero h1 {
    font-size: clamp(2.5rem, 7vw, 5.5rem);
    font-weight: 700;
    letter-spacing: -2px;
    line-height: 1.0;
    color: #1a1a1a;
    margin: 0 0 14px;
    opacity: 0;
    transform: translateY(20px);
    animation: heroFadeUp 0.8s ease 0.5s forwards;
  }
  .hero-name-wild { color: var(--orange); }

  /* ── SCROLL INDICATOR ──────────────── */
  .scroll-hint {
    margin-top: 24px;
    display: flex; flex-direction: column; align-items: center; gap: 6px;
    opacity: 0;
    animation: hintFadeIn 0.6s ease 1.4s forwards;
  }
  .scroll-hint span {
    font-size: 8px; letter-spacing: 3px; text-transform: uppercase;
    color: rgba(0,0,0,0.35);
    font-weight: 400;
  }
  .scroll-hint::after {
    content: '';
    width: 1px; height: 24px;
    background: linear-gradient(to bottom, var(--orange), transparent);
    animation: scrollPulse 2s ease-in-out 1.8s infinite;
  }

  @keyframes heroFadeUp { to { opacity: 1; transform: translateY(0); } }
  @keyframes hintFadeIn { to { opacity: 1; } }
  @keyframes scrollPulse {
    0%, 100% { opacity: 0.3; transform: scaleY(1); }
    50% { opacity: 0.8; transform: scaleY(1.3); }
  }

  @media (max-width: 700px) {
    .hero {
      padding: 0 1.25rem 32px;
      min-height: 56vh;
      padding-top: 28vh;
    }
    .hero h1 {
      letter-spacing: -1.5px;
      font-size: clamp(2.4rem, 11vw, 3.6rem);
      margin-bottom: 10px;
      line-height: 0.95;
    }
    .scroll-hint { margin-top: 40px; opacity: 1; animation: none; }
    .scroll-hint::after { animation: scrollPulse 2s ease-in-out infinite; }
  }
  @media (max-width: 400px) {
    .hero { padding-top: 26vh; }
    .hero h1 { letter-spacing: -1px; font-size: 2.2rem; }
  }
</style>
