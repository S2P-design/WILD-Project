
<div class="hero">
  <img src="/basecamp/brand/logos/wp-monogram-orange.png" alt="Wild Project" class="hero-logo" />
  <h1>
    <span class="hero-word-wild">WILD</span> <span class="hero-word-project">PROJECT</span>
  </h1>
  <div class="hero-profile">
    <img src="/brand-assets/rene-wild.jpg" alt="René Wild" />
  </div>
</div>

<style>
  .hero {
    display: flex; flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    padding: 22vh 3rem 40px;
    min-height: 62vh;
    position: relative;
    text-align: center;
    background: #F5F5F3;
    z-index: 1;
  }
  .hero-logo {
    width: 3cm;
    height: auto;
    margin-bottom: 20px;
    opacity: 0;
    transform: translateY(-48px);
    animation: heroFromTop 0.8s cubic-bezier(.5,.1,.2,1) 0.2s forwards;
  }
  .hero-profile {
    width: 96px;
    height: 96px;
    border-radius: 50%;
    overflow: hidden;
    margin-top: 28px;
    border: 2px solid var(--orange);
    box-shadow: 0 4px 18px rgba(0,0,0,0.08);
    background: #F5F5F3;
    opacity: 0;
    transform: translateY(20px);
    animation: heroFromBottom 0.9s cubic-bezier(.5,.1,.2,1) 1.4s forwards;
    cursor: pointer;
    transition: box-shadow 0.45s ease;
  }
  .hero-profile img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 22%;
    transform: scale(1.35);
    transform-origin: center 35%;
    transition: transform 0.55s cubic-bezier(.2,.7,.2,1);
    display: block;
  }
  .hero-profile:hover {
    box-shadow: 0 8px 28px rgba(252,80,0,0.18);
  }
  .hero-profile:hover img {
    transform: scale(1.5);
  }
  @keyframes heroFromBottom {
    to { opacity: 1; transform: translateY(0); }
  }
  .hero h1 {
    font-size: clamp(2.5rem, 7vw, 5.5rem);
    font-weight: 700;
    letter-spacing: -2px;
    line-height: 1.0;
    color: #1a1a1a;
    margin: 0;
    overflow: hidden;
  }
  .hero h1 .hero-word-wild,
  .hero h1 .hero-word-project {
    display: inline-block;
    opacity: 0;
  }
  .hero h1 .hero-word-wild {
    color: var(--orange);
    transform: translateX(-80px);
    animation: heroFromLeft 0.9s cubic-bezier(.5,.1,.2,1) 0.65s forwards;
  }
  .hero h1 .hero-word-project {
    transform: translateX(80px);
    animation: heroFromRight 0.9s cubic-bezier(.5,.1,.2,1) 0.65s forwards;
  }

  @keyframes heroFromTop {
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes heroFromLeft {
    to { opacity: 1; transform: translateX(0); }
  }
  @keyframes heroFromRight {
    to { opacity: 1; transform: translateX(0); }
  }

  @media (max-width: 700px) {
    .hero {
      padding: 28vh 1.25rem 32px;
      min-height: 56vh;
    }
    .hero h1 {
      letter-spacing: -1.5px;
      font-size: clamp(2.4rem, 11vw, 3.6rem);
      line-height: 0.95;
    }
    .hero-word-wild { transform: translateX(-50px); }
    .hero-word-project { transform: translateX(50px); }
    .hero-logo { transform: translateY(-32px); }
  }
  @media (max-width: 400px) {
    .hero { padding-top: 26vh; }
    .hero h1 { letter-spacing: -1px; font-size: 2.2rem; }
  }
  @media (prefers-reduced-motion: reduce) {
    .hero-logo, .hero-word-wild, .hero-word-project {
      animation: none !important;
      opacity: 1 !important;
      transform: none !important;
    }
  }
</style>
