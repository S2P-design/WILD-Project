
<footer class="footer">
  <div class="footer-inner">
    <div class="footer-left">
      <p class="copyright">&copy; 2026 Wild Project — René Wild</p>
      <p class="footer-links">
        <a href="/impressum">Impressum</a>
        <span class="sep">&middot;</span>
        <a href="/datenschutz">Datenschutz</a>
        <span class="sep">&middot;</span>
        <a href="/card">Digitale Visitenkarte</a>
      </p>
    </div>
    <div class="footer-right">
      <img src="/wp-logo.png" alt="WP" class="footer-logo">
      <p class="footer-tagline">LIVE BY DESIGN, NOT BY DEFAULT.</p>
      <p class="footer-coords">47.8561° N &nbsp; 12.1289° E</p>
    </div>
  </div>
</footer>

<style>
  .footer {
    padding: 28px 3rem 32px;
    background: linear-gradient(180deg, transparent 0%, rgba(0,0,0,0.85) 100%);
    padding-top: 40px;
    position: relative;
  }
  .footer-inner {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
  }

  /* ── Links: Copyright + Links ── */
  .footer-left {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  .copyright {
    font-size: 11px;
    color: rgba(255,255,255,0.6);
    letter-spacing: 0.5px;
  }
  .footer-links {
    font-size: 11px;
    letter-spacing: 0.5px;
  }
  .footer-links a {
    color: rgba(255,255,255,0.55);
    text-decoration: none;
    transition: color 0.2s;
  }
  .footer-links a:hover { color: var(--orange); }
  .footer-links .sep {
    color: rgba(255,255,255,0.2);
    margin: 0 5px;
  }

  /* ── Rechts: Logo + Claim + Coords ── */
  .footer-right {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 6px;
  }
  .footer-logo {
    width: 24px;
    height: 24px;
    opacity: 0.6;
    transition: opacity 0.3s;
    margin-bottom: 2px;
  }
  .footer-logo:hover { opacity: 0.9; }
  .footer-tagline {
    font-family: monospace;
    font-size: 9px;
    font-weight: 400;
    letter-spacing: 2px;
    color: rgba(255,255,255,0.4);
    overflow: hidden;
    border-right: 2px solid var(--orange);
    white-space: nowrap;
    width: 0;
    animation: typing 2.5s steps(32) 0.5s forwards, blink 0.6s step-end infinite 0.5s;
  }
  @keyframes typing {
    to { width: 25.5em; }
  }
  @keyframes blink {
    50% { border-color: transparent; }
  }
  .footer-coords {
    font-size: 7px;
    font-weight: 300;
    letter-spacing: 1.5px;
    color: rgba(255,255,255,0.35);
    font-variant-numeric: tabular-nums;
  }

  /* ── Light Mode ── */
  :global(body.light) .footer {
    background: linear-gradient(180deg, transparent 0%, rgba(245,245,243,0.9) 100%);
  }
  :global(body.light) .copyright { color: rgba(0,0,0,0.45); }
  :global(body.light) .footer-links a { color: rgba(0,0,0,0.4); }
  :global(body.light) .footer-links a:hover { color: var(--orange); }
  :global(body.light) .footer-links .sep { color: rgba(0,0,0,0.15); }
  :global(body.light) .footer-tagline { color: rgba(0,0,0,0.3); border-right-color: var(--orange); }
  :global(body.light) .footer-coords { color: rgba(0,0,0,0.2); }

  /* ── Mobile ── */
  @media (max-width: 700px) {
    .footer { padding: 20px 1.5rem 36px; }
    .footer-inner {
      flex-direction: column;
      align-items: center;
      text-align: center;
      gap: 20px;
    }
    .footer-left { align-items: center; gap: 8px; }
    .footer-right { align-items: center; }
    .copyright { font-size: 10px; }
    .footer-links { font-size: 10px; }
    .footer-tagline { font-size: 8px; letter-spacing: 1.5px; }
    .footer-coords { font-size: 7px; }
    .footer-logo { width: 22px; height: 22px; }
  }
</style>
